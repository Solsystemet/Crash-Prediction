[project]
name = "crash-prediction"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy>=1.19.1",
    "pandas>=3.0.1",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
    "torch>=2.10.0",
    "torchvision>=0.25.0",
]
